server:
  port: 8080

database:
  dsn: "postgres://postgres:postgres@localhost:5432/agentfi?sslmode=disable"

redis:
  url: "redis://localhost:6379"

llm:
  api_key: ""
  api_url: "https://api.openai.com/v1"
  model: "gpt-4o"
  temperature: 0.7
  max_tokens: 4096

auth:
  jwt_secret: "change-me-in-production"

log:
  level: "info"
